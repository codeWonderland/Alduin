extends ../../node_modules/jade-bootstrap/layouts/starter

append styles
    link(href='/stylesheets/creators/profile.min.css', rel='stylesheet')

append body
    div#creator-container
    div(style={ "text-align" : "center"})
        a.btn(href="/") Back to Home
    script(src='/javascripts/creators/profile.min.js')
    script.
        var user = !{user}